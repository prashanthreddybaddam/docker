sudo : required

services:
        - docker
before-install:
        - docker build prashanthreddy023/hello .

script:
        - docker run prashanthreddy023/hello

after-success:
        - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
        - docker push prashanthreddy023/hello
